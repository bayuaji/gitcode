@model IEnumerable<GitCode.Models.DetailTeam>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.User.Username)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Team.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsOwner)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.User.Username)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Team.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IsOwner)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DetailTeamId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DetailTeamId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DetailTeamId })
        </td>
    </tr>
}

</table>
